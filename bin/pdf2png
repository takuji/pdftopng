#! /usr/bin/env ruby

require 'pathname'
require 'pdf2png'

options = {
  work_dir: Pathname.new("./out")
}

# checks if pdftoppm and convert are installed.
# TODO: implement this

# main
pdf_path = Pathname.new(ARGV[0]).realpath
Pdf2png.convert(options[:work_dir], pdf_path)
